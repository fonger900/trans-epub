[project]
name = "trans-epub"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ebooklib>=0.18",
    "beautifulsoup4>=4.12",
    "lxml>=5.2",
    "requests>=2.32",
    "python-dotenv>=1.0",
    "rich>=13",
]

[project.scripts]
trans-epub = "trans_epub.cli:main"
